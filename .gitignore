# Dependencies
node_modules/
.pnpm-store/

# Environment
.env
.env.local
.env.*.local

# Build outputs
dist/
build/

# Logs & OS
*.log
.DS_Store

# Local-only docs (feature overview, OpenAPI spec for Postman)
docs/
# Screen-by-screen description of what each screen does and why, and the
# manual test script that goes with it. Kept out of the repository
# deliberately, like docs/ above.
SCREENS.md
TEST-THU-CONG.md

# Vercel CLI linking state — machine-local, and `vercel pull` writes
# fetched environment variables next to it.
.vercel/
.vercel
.env*
